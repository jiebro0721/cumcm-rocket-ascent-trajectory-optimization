# 项目审阅报告：两级运载火箭发射轨迹建模与仿真

> 审阅日期：2026-08-28
> 审阅范围：`src/` 全部源代码、`results/` 数值结果、`figures/` 与 `figures_svg/` 图表、`论文/main.tex` 论文正文及附录、`README.md` 与两份解题文档

---

## 一、总体评价

本项目完整覆盖了题目四个问题，从动力学建模、基准仿真、入轨打靶到燃料最优控制，形成"代码—结果—图表—论文"闭环。物理建模严谨（地球自转、指数大气、相对速度阻力、质量流出），数值方法得当（DOP853 高阶自适应积分、多初值打靶、SLSQP 控制参数化），论文结构规范、推导清晰。整体属于高质量参赛项目。

发现 **1 处关键数据错误**（论文表4速度值）、**3 处中等问题**（建模选择、文档不一致、规划文档过时）、**5 处轻微问题**（调试残留、代码风格），以下按严重程度排列。

---

## 二、优点

1. **物理建模完备且量纲一致**。惯性 ECI 笛卡尔坐标系避免了极坐标奇点；地球自转三处自洽体现（初速 465 m/s、阻力用 v_rel、入轨用惯性速度）；指数大气与质量流出由真空比冲严格确定。代码中 `common.py` 集中管理物理常数与火箭参数，四个问题共享，避免不一致。

2. **数值方法选择合理**。DOP853（8阶）配合 rtol=1e-9 与事件检测，兼顾精度与效率；问题2用三维打靶（fsolve + 196组网格初值）；问题3/4用控制参数化 + SLSQP + 多初值并行搜索，适合非凸问题。

3. **优化器—验证器一致性**。项目开发中已发现并修复了"内部积分器与最终 Simulator 控制律参数化不一致导致伪残差"的问题，所有最优解均用完整 Simulator 端到端复验，入轨残差 < 1e-5 才接受。这是工程实践中的好习惯。

4. **分析与验证充分**。包含节点数收敛性实验（3/5/7/9 节点）、节流下限灵敏度扫描、滑行时间扫描表、理想 ΔV 能量分析，使结论有物理可解释性。

5. **论文写作规范**。摘要加粗关键结果，问题分析→模型假设→符号说明→四问求解→模型评价，结构完整；参考文献7篇涵盖中外文献且与代码注释对应。

6. **数值结果经独立核算验证一致**。燃尽时刻 t1=149.06 s、tb2=425.61 s、圆轨道速度 7672.60 m/s、分离后质量 80000 kg、Q2→Q3 节省 276.5 kg 等关键数值均与代码/CSV 交叉吻合。理想 ΔV = 10342 m/s 与需求 7208 m/s 之差 3134 m/s 的能量分析正确。

---

## 三、问题与建议

### 【关键】问题 A：论文表4"一子级燃尽"速度数据错误

**位置**：`论文/main.tex` 表4（`\ref{tab:q1res}`，约第 280 行）

**现象**：表中"一子级燃尽"行惯性速度标注为 **2795.6 m/s**，但 CSV 实际仿真值为 **3260.19 m/s**。差值 464.6 m/s 恰等于海平面地球自转线速 ω_E·R_E，即误将相对速度（惯性速度 − 地球自转速度）当作惯性速度填入。

**交叉佐证**：同一篇论文的表5（滑行时间扫描，`\ref{tab:tcsweep}`）在 t_c=0 行正确列出 3260.2 m/s，与 CSV 一致。因此表4与表5内部自相矛盾。

**影响**：虽不影响 ΔV 理想分析（该分析基于火箭方程而非实际燃尽速度），但表4是问题1的核心结果表，数据错误会降低评审信任度。

**建议**：将表4"一子级燃尽"惯性速度从 2795.6 改为 **3260.2 m/s**。

---

### 【中等】问题 B：二级推力方向使用相对速度而非惯性速度

**位置**：`src/common.py` `simulate()` 方法，`controller=None` 分支；以及 `q2/q3/q4` 的 `phi0_of()`

**现象**：题目说"推力方向始终与速度方向保持一致"（问题1）和"初始俯仰角与速度方向一致"（问题2）。代码统一使用**相对大气速度** v_rel = v − ω×r 确定推力方向。但二子级分离后气动阻力已被忽略（`drag_after_sep=False`），物理上不存在"相对大气"的参照。在点火时刻（h约210 km），v_rel 与 v_inertial 的路径角差约 5°，使推力偏向更陡的方向。

**影响评估**：
- 问题1仅是基准仿真（定性结论"过冲"不变），但具体数值（h=484.83 km 等）会与使用惯性速度的结果有偏差；
- 问题2–4中 phi0 虽由 v_rel 计算，但优化器可自由调整后续俯仰角节点，入轨可行性不受影响，仅影响具体控制参数值。

**建议**：在论文中补充一句说明"速度方向取相对大气速度，以保持与一子级阻力建模的一致性"即可增强自洽性；或改为惯性速度并重新运行。两者均可接受，关键是明确说明。

---

### 【中等】问题 C：解题文档残差数据过时

**位置**：`第二期火箭培训_解题文档.md` 第三节

**现象**：文档声称问题2"完整复仿端到端 ~1.3e-5（对应 h 误差 0.010 km、V 误差 0.022 m/s、γ 误差 0.0007°）"，但 `results/q2_summary.csv` 实际显示 h 误差 8.6e-7 km、V 误差 2.1e-5 m/s、γ 误差 1.1e-9°，残差约 1.4e-10。论文 `main.tex` 正确写为 10^-10 量级。

**建议**：更新解题文档中的残差数据，使其与 CSV 和论文一致。

---

### 【中等】问题 D：规划文档推荐方案与实际实现不一致

**位置**：`第二期火箭培训-4问模型与求解算法总结.md`

**现象**：该文档推荐"方案C（相对地速极坐标 [r,θ,V,γ,m]）"作为主方案，将"方案B（惯性 ECI 笛卡尔）"列为备选交叉验证。但实际代码与论文均采用方案B。规划文档未更新以反映最终决策。

**建议**：将文档中"推荐方案C"的表述更新为"最终采用方案B"，或注明方案C为初版规划、实现时改为方案B。

---

### 【轻微】问题 E：问题3主程序残留调试输出

**位置**：`src/q3_fuel_opt.py` `main()` 函数末尾

**现象**：存在 `[dbg] phi0_match=...` 与 `[dbg] evaluate res=...` 两行调试打印，以及 `import numpy as _np` 未被使用。

**建议**：删除调试行后再提交。

---

### 【轻微】问题 F：硬编码重力加速度

**位置**：`src/q2_inscription.py`、`q3_fuel_opt.py`、`q4_throttle_opt.py` 的 `stage2_end()` 方法

**现象**：质量流量公式中硬编码 `9.80665`，而 `common.py` 已定义 `G0 = 9.80665` 并在各文件中导入。值相同无 bug，但风格不一致。

**建议**：统一使用 `G0` 常量。

---

### 【轻微】问题 G：代码注释残留思考过程

**位置**：`src/q3_fuel_opt.py` `stage2_end()` 方法

**现象**：注释 `# phi 节点值与 phi0 拼接：第一段起点即 phi0 -> 用 (phi0, phi_nodes) 长度 n+1？` 和 `# 更简单：...` 是开发中的思考痕迹。

**建议**：清理为简洁说明。

---

### 【轻微】问题 H：入轨残差第三分量归一化与论文公式略有出入

**位置**：`src/common.py` `orbit_residual()` vs 论文式 (eq:resid)

**现象**：论文写第三分量为 r·v / (a·sqrt(μ/a))（用目标半径 a），代码写 rdot / v_circ = (r·v/r) / v_circ（用实际半径 r）。在解附近 r 约等于 a 故无实质影响，但严格说不一致。

**建议**：统一为论文公式（用 a）或更新论文公式（用 r）。

---

## 四、数值核查汇总

以下关键数值经独立核算或与 CSV 交叉验证，结果如下：

| 核查项 | 文档/论文值 | 实际值 | 结论 |
|:---|:---|:---|:---|
| 一子级燃尽时刻 t1 | 149.06 s | 149.061 s | 通过 |
| 二子级燃尽时长 tb2 | 425.61 s | 425.61 s | 通过 |
| 圆轨道速度 v_circ | 7672.60 m/s | 7672.599 m/s | 通过 |
| Q1 关机高度 | 484.83 km | 484.827 km | 通过 |
| Q1 关机速度 | 8432.51 m/s | 8432.510 m/s | 通过 |
| Q1 一子级燃尽速度 | 2795.6 m/s | 3260.19 m/s | 不通过（问题A） |
| Q2 滑行时间 | 113.86 s | 113.859 s | 通过 |
| Q2 入轨残差 | 10^-10 | 1.36e-10 | 通过 |
| Q2 剩余推进剂 | 3873 kg | 3872.96 kg | 通过 |
| Q3 消耗推进剂 | 57850.5 kg | 57850.54 kg | 通过 |
| Q3 vs Q2 节省 | 276.5 kg | 276.5 kg | 通过 |
| Q4 节流律 | σ恒等于1.0 | 全1.0000 | 通过 |
| 滑行段能量守恒 | — | dE=0.000 | 通过 |
| ΔV_ideal | 10342 m/s | 10343 m/s | 通过（四舍五入） |

---

## 五、总体结论

项目建模扎实、实现完整、论文规范，具备竞赛获奖水平的基础。主要风险点是论文表4的一子级燃尽速度数据错误（问题A），建议提交前修正。其余问题均为建模选择的说明补充或文档同步类工作，不影响结论正确性。

**优先级建议**：A（必改）→ B/C/D（建议改）→ E/F/G/H（提交前清理）。
